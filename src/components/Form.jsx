const Form = () => {
  return (
    <section>
      <form>
        <input type="text" placeholder="Add item" />
        <button type="submit">Add Item</button>
      </form>
    </section>
  );
};

export default Form;
